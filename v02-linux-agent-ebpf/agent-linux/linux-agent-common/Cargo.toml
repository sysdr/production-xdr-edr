[package]
name = "linux-agent-common"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Shared ExecEvent ABI for linux-agent eBPF and userspace"

[features]
default = ["std"]
std = []

[dependencies]
